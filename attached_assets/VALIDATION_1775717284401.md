# Validation notes

## Verified in this environment

### API
- `cd apps/api && npm install`
- `cd apps/api && npm run build`
- smoke-tested endpoints:
  - `GET /health`
  - `POST /auth/login`
  - `GET /dashboard/summary`
  - `POST /shares`
  - `POST /reservations`
  - `POST /guests`
  - `POST /access/open`
  - `GET /admin/overview`

### Admin
- `cd apps/admin && npm install`
- `cd apps/admin && npm run build`

### Mobile
- Source code completed for Expo / React Native.
- Structural TypeScript check executed with local `tsconfig.check.json` and ambient module stubs because the container's npm mirror returned `401` for selected React Native tarballs during full Expo install.
- The runtime package manifest remains configured for real Expo dependencies so the app can be installed on a normal machine with public npm access.

## Important
This archive is intentionally delivered as source code, without `node_modules`, to keep it small and portable.
